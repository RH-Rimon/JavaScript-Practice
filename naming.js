let location = 'BBan';
let age = 18;
let boyosh = 21;

// 1.  variable name should be something meaningful, it will indicate what it contains

let avc = 35;
let info = 'mon majire'

// do not use shorthand
// it's okay if the name is longer

// 2.  can not use a keyword
// 3.  has to be in one word. can not contain any space or gap.. Can use underscore

const her_phone_number = 55555555555;  // snake-case
const herPhoneNumber = 55555555555;  // camel-case
const HerPhoneNumber = 55555555555;  // pascal-case

// 5.  name can't start with a number but number could be used anywhere other than the first letter

const person3 = 6;
const p3erson = 7;

// 5.  other than letter name can start with either _ or $ sign . can't use any other special character

// 6. case sensitive

let address = 'katabon';
let Address = 'bandorbon';