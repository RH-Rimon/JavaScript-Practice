const numbers = [1,2,3,5,6,77,8,8,9,90];

const length = numbers.length;

for(let i = 0;i < numbers.length; i++){

    console.log('index value:', i);

    const item = numbers[i];

    console.log('item:',item);
}